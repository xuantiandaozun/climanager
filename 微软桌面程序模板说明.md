# Windows 桌面应用模板总结

本文档不再围绕当前业务功能展开，而是把本仓库沉淀为一套可复用的 `Windows 桌面应用模板` 说明。目标是让后续新项目只要参考这一份文档，就能快速理解：

- 项目框架怎么搭
- 前端和 Rust/Tauri 怎么分层
- 目录应该怎么组织
- `MSIX` 怎么打包
- 微软商店上架时要注意什么
- 图标和 Logo 资源怎么批量生成
- 文档、开发日志、发布流程怎么沉淀

适用对象：

- 基于 `Tauri 2 + Vue 3 + Rust` 开发的 Windows 桌面程序
- 目标分发方式包含 `MSIX` 或微软应用商店
- 希望形成一套长期可维护、可复制的项目模板

## 1. 模板核心定位

这是一个偏工程化的 Windows 桌面程序模板，不是单纯的前端脚手架。

它同时解决了四类问题：

1. 应用框架：前端、桌面壳、Rust 能力层如何组织
2. 工程结构：目录、配置、脚本、文档如何落地
3. 打包分发：如何从源码构建出 `.msix`
4. 上架交付：如何满足微软商店提交的基本要求

这套模板的核心思想是：

- UI 层继续使用现代 Web 技术栈提高开发效率
- 桌面容器使用 Tauri 2 保持轻量化
- 本地系统能力放在 Rust 层
- 商店分发使用独立的 `MSIX` 打包链路，不依赖图形化工具
- 所有关键步骤脚本化、文档化、可进入 CI

## 2. 技术栈总览

当前模板的核心技术栈如下：

| 层级 | 技术 |
|---|---|
| 桌面壳 | `Tauri 2` |
| 前端 | `Vue 3` + `TypeScript` + `Vite` |
| 路由 | `vue-router` |
| UI 组件 | `Naive UI` |
| 多语言 | `vue-i18n` |
| 本地能力 | Rust + Tauri commands |
| 打包 | `MakeAppx.exe` + `AppxManifest.xml` |
| 本地签名 | `SignTool.exe` |
| CI | `GitHub Actions` |

补充说明：

- `Tauri` 负责把前端页面包装成桌面程序，并提供 IPC 能力
- `Vue 3` 负责桌面应用 UI 开发
- `Rust` 负责文件、系统、性能相关和平台能力逻辑
- `MSIX` 打包不依赖 Tauri 官方直接输出，而是自建脚本链

## 3. 为什么这套技术栈适合做 Windows 桌面应用

### 3.1 为什么用 Tauri 2

适合原因：

- 安装包体积更可控
- 资源占用通常低于 Electron
- 安全模型更清晰
- Rust 更适合承接本地能力与性能逻辑
- 仍然可以复用成熟的 Web 前端开发方式

对模板项目而言，最大的价值不是“跨平台”三个字，而是：

- Windows 桌面项目可以快速起步
- 前端团队也能高效参与
- 能保留后续扩展桌面能力的空间

### 3.2 为什么用 Vue 3 + Vite

适合原因：

- 研发效率高
- 组件化清晰
- TypeScript 集成成熟
- 构建速度快
- 对工具类、后台类、配置类应用非常友好

### 3.3 为什么保留 Rust 能力层

模板项目里，Rust 层不是可有可无，而是重要边界：

- 文件系统处理
- 图片、压缩、解析类能力
- 系统 API 调用
- 与平台相关的能力实现
- 未来性能敏感模块

建议把 Rust 看成“桌面能力服务层”，而不是只当 Tauri 的附属配置目录。

## 4. 项目架构

## 4.1 分层架构

建议把项目明确拆成三层：

1. 表现层：Vue 页面、组件、布局、交互
2. 应用层：状态、路由、国际化、业务编排、前端调用封装
3. 桌面能力层：Tauri command、Rust 服务、文件/系统/平台逻辑

可抽象成下面这个关系：

```text
Vue Views / Components
  -> composables / services / router / i18n
  -> invoke()
Tauri IPC
  -> Rust commands
  -> system services / file / native integrations
```

这个分层的目的不是“好看”，而是为了减少耦合：

- Vue 不直接承担本地能力细节
- Rust 不参与页面展示和路由逻辑
- IPC 边界尽量保持稳定、简单

### 4.2 当前模板中的实际对应关系

当前仓库中能直接映射到这套架构：

- `src/main.ts`：应用入口、Router、i18n 装配
- `src/App.vue`：全局布局、主题、导航、语言切换
- `src/views/`：页面层
- `src/composables/`：组合式逻辑、全局能力封装
- `src-tauri/src/lib.rs`：Tauri Builder、插件注册、command 暴露
- `src-tauri/src/commands/`：Rust 命令实现

在 `src-tauri/src/lib.rs` 里，当前模板采用的是统一注册 command 的方式：

```rust
tauri::Builder::default()
    .plugin(tauri_plugin_dialog::init())
    .plugin(tauri_plugin_fs::init())
    .invoke_handler(tauri::generate_handler![
        // commands...
    ])
```

这类结构适合模板化复用，因为：

- 插件注册位置固定
- command 出入口集中
- 后续增加桌面能力时不容易失控

### 4.3 推荐边界约定

后续项目建议长期保持以下规则：

- 页面负责展示与交互，不直接写平台细节
- composable/service 负责组织调用
- Rust command 只暴露稳定接口
- IPC 参数尽量简单，优先使用基础类型和结构化对象
- 字节流、路径、配置、结果数据在接口层统一约定

不建议的做法：

- 在 Vue 组件里直接堆大量平台判断
- Rust command 与某个页面结构强绑定
- 一个 command 既做 UI 决策又做系统操作

## 5. 目录结构模板

推荐把后续项目组织成下面这类结构：

```text
.
|- src/                          # 前端应用
|  |- main.ts                    # 应用入口
|  |- App.vue                    # 全局外壳
|  |- views/                     # 页面层
|  |- components/                # 通用组件
|  |- composables/               # 组合式逻辑
|  |- services/                  # 调用封装、API、bridge
|  |- locales/                   # 多语言资源
|  `- utils/                     # 通用工具
|
|- src-tauri/                    # Tauri + Rust
|  |- src/
|  |  |- lib.rs                  # Tauri builder
|  |  |- main.rs                 # 桌面入口
|  |  |- commands/               # command 层
|  |  `- services/               # Rust 内部服务层
|  |
|  |- capabilities/              # Tauri capability 配置
|  |- icons/                     # Tauri 图标资源
|  |- msix/                      # MSIX 模板与资源
|  |  |- AppxManifest.xml.tpl
|  |  `- Assets/
|  |
|  |- Cargo.toml
|  `- tauri.conf.json
|
|- scripts/
|  `- msix/                      # MSIX 打包、签名、资源生成脚本
|
|- docs/
|  |- architecture.md            # 架构文档
|  |- msix-packaging.md          # 打包文档
|  `- dev-log/                   # 决策日志 / 踩坑日志
|
`- .github/workflows/            # CI / Release
```

这套结构的优势：

- 前端和 Rust 边界清楚
- 打包脚本和业务代码分离
- 文档体系和代码体系并行维护
- 新项目复制后，替换业务代码即可继续使用

## 6. 关键配置文件说明

### 6.1 `package.json`

这是前端与 Tauri 开发命令的统一入口。

当前模板中已包含：

- `pnpm dev`
- `pnpm dev:desktop`
- `pnpm build`
- `pnpm build:desktop`
- `pnpm typecheck`
- `pnpm test:smoke`
- `pnpm icons:generate`

模板建议：

- 所有开发、构建、校验命令统一收口到 `package.json`
- 不要把关键打包步骤只藏在本地 IDE 配置里

### 6.2 `src-tauri/tauri.conf.json`

这里主要定义：

- `productName`
- `version`
- `identifier`
- 开发环境前端地址
- 构建前命令
- Tauri 默认 bundler 配置

要注意：

- 这里的 `identifier` 是应用基础标识，不等于最终 `MSIX Identity Name`
- Tauri 自带 bundler 可用于常规安装包，但商店上架仍建议走独立 `MSIX` 流程

### 6.3 `Cargo.toml`

这里定义 Rust 依赖和 crate 配置。

模板建议：

- 桌面能力相关依赖按模块拆分
- 通用序列化使用 `serde`
- 与系统、文件、图像、压缩、网络等相关依赖集中管理

### 6.4 `AppxManifest.xml.tpl`

这是 `MSIX` 的核心清单模板。

当前模板已经包含这些关键元素：

- `<Identity>`
- `<Properties>`
- `<Dependencies>`
- `<Resources>`
- `<Applications>`
- `<uap:VisualElements>`
- `<Capabilities>`

它本质上决定了：

- 应用身份
- 展示名称
- 包版本
- 支持平台
- 图标资源
- 能力声明

这份文件应该被视为“上架配置核心文件”，不是普通资源文件。

## 7. 开发流程模板

### 7.1 本地开发

常规开发流程：

```bash
pnpm install
pnpm dev:desktop
```

前端变更由 Vite 驱动，桌面程序由 Tauri 装载。

### 7.2 基础校验

建议每次提交前至少执行：

```bash
pnpm typecheck
pnpm test:smoke
```

如果后续模板继续扩展，建议补充：

- Rust `cargo check`
- 前端单测
- 打包冒烟验证

### 7.3 产物构建

模板中的构建分为两步理解：

1. 应用构建：生成桌面可执行程序
2. 商店打包：把可执行程序包装成 `MSIX`

不要把这两个步骤混为一谈。

## 8. MSIX 打包实现

## 8.1 当前采用的策略

当前模板采用的是“手动可控的标准 MSIX 流程”：

1. 先执行 `pnpm tauri build --no-bundle`
2. 得到可执行文件
3. 使用 `stage.ps1` 组装 MSIX 目录结构
4. 使用 `pack.ps1` 调用 `MakeAppx.exe` 生成 `.msix`
5. 本地需要侧载时，再用 `sign-dev.ps1` 签名

这样做的原因：

- 流程稳定
- 适合 CI 自动化
- 所有打包行为透明可控
- 不依赖图形化工具点击操作

### 8.2 打包链路图

```text
源码
  -> pnpm tauri build --no-bundle
  -> 生成 exe
  -> scripts/msix/stage.ps1
  -> 生成 build/msix-stage/
  -> scripts/msix/pack.ps1
  -> 生成 build/*.msix
  -> scripts/msix/sign-dev.ps1 (仅本地测试需要)
```

### 8.3 `stage.ps1` 在做什么

当前脚本做了这几件事：

- 定位 `src-tauri/target/release/*.exe`
- 创建 `build/msix-stage`
- 复制主程序
- 复制 `src-tauri/msix/Assets` 中的资源
- 将 `AppxManifest.xml.tpl` 放入 staging 目录作为 `AppxManifest.xml`

也就是说，`stage.ps1` 负责把零散产物整理为标准 MSIX 目录。

建议后续项目延续这个脚本，而不是直接让打包脚本同时承担“找文件 + 拷贝 + 组装 + 压包”的所有职责。

### 8.4 `pack.ps1` 在做什么

当前脚本会：

- 自动搜索本机 Windows SDK 中的 `MakeAppx.exe`
- 对 `build/msix-stage` 执行打包
- 输出到 `build/<YourApp>.msix`

这一步是正式压制包体。

模板建议：

- 保持自动探测 Windows SDK 路径
- 兼容不同机器的 SDK 安装位置
- 打包输出目录固定，方便 CI 收集产物

### 8.5 `sign-dev.ps1` 在做什么

当前脚本会：

- 自动搜索 `SignTool.exe`
- 使用本地 `dev.pfx` 和密码对 `.msix` 签名

这个脚本的定位非常明确：

- 仅用于开发机本地侧载测试
- 不等于微软商店最终签名

### 8.6 为什么不依赖图形化 MSIX Packaging Tool

因为模板项目最重要的是“可复制、可自动化、可进 CI”。

图形化工具的问题是：

- 人工步骤太多
- 难以复现
- 难以固化到仓库
- 难以接入自动发布流程

所以模板必须采用脚本化方案。

## 9. Logo 与图标资源生成

这是当前模板里非常值得复用的一块。

### 9.1 为什么要单独做脚本

Windows 桌面程序和 `MSIX` 对图标要求远比普通 Web 项目严格：

- 不同用途尺寸不同
- 不同 DPI 需要 scale 变体
- Tauri 自己也需要一套图标
- 商店、开始菜单、任务栏、启动画面用到的资源并不相同

如果每次都手工切图，后续项目一定会出错。

### 9.2 当前脚本位置

图标资源生成脚本在：

- `scripts/msix/generate-assets.ps1`

对应命令：

```bash
pnpm icons:generate
```

### 9.3 当前脚本的输入与输出

默认输入：

- `logo.png`

输出到两个地方：

1. `src-tauri/msix/Assets/`
2. `src-tauri/icons/`

也就是说，一份源 Logo 同时服务于：

- `MSIX` 打包资源
- Tauri 应用图标资源

### 9.4 当前脚本生成了什么

MSIX 资源部分包括：

- `StoreLogo.png`
- `Square44x44Logo.png`
- `Square71x71Logo.png`
- `Square150x150Logo.png`
- `Square310x310Logo.png`
- `Wide310x150Logo.png`
- `SplashScreen.png`

并且自动生成：

- `scale-100`
- `scale-125`
- `scale-150`
- `scale-200`
- `scale-400`

Tauri 图标部分包括：

- `32x32.png`
- `64x64.png`
- `128x128.png`
- `128x128@2x.png`
- `icon.png`
- `source.png`

### 9.5 当前脚本实现方式

脚本使用 PowerShell + `System.Drawing`：

- 读取原始图片
- 按目标尺寸等比缩放
- 居中绘制到透明背景画布
- 批量输出 PNG

这套脚本对模板非常实用，因为它解决的是“资源标准化”问题，而不是单纯缩放图片。

### 9.6 模板建议

后续新项目建议继续保持以下做法：

- 统一只维护一个高分辨率源 Logo
- 通过脚本批量生成所有平台资源
- 把图标资源生成纳入项目标准命令
- 不手工维护几十个不同尺寸的图片文件

## 10. MSIX Manifest 关键字段说明

`AppxManifest.xml.tpl` 是后续模板项目最需要认真维护的文件之一。

### 10.1 Identity

关键字段：

- `Name`
- `Publisher`
- `Version`
- `ProcessorArchitecture`

注意：

- 本地调试和商店发布时，这些值可能不同
- `Publisher` 必须与签名证书或商店分配值严格一致

### 10.2 Properties

关键字段：

- `DisplayName`
- `PublisherDisplayName`
- `Logo`
- `Description`

这部分会影响：

- 安装展示
- 商店展示
- 系统中的应用信息

### 10.3 Dependencies

这里声明目标系统范围，例如：

- `Windows.Desktop`
- 最低支持版本
- 测试验证版本

模板建议：

- 最低版本要结合 Tauri、WebView2 和实际测试结果决定
- 不要随意写太高，否则会缩小可安装范围

### 10.4 Resources

当前模板中已声明：

- `en-US`
- `zh-CN`
- `ja-JP`

这表示包在资源层面已经准备好多语言支持。

### 10.5 Applications 与 VisualElements

这里定义：

- 主程序入口 exe
- 开始菜单和磁贴使用的图标
- ShortName
- Description

这一段和图标资源强相关，后续换项目时必须一起核对。

### 10.6 Capabilities

当前模板声明了：

```xml
<rescap:Capability Name="runFullTrust" />
```

这意味着它按全信任桌面应用方式运行。

适用场景：

- Tauri 桌面程序
- 需要传统桌面能力访问的应用

风险点：

- 商店审核时可能需要解释用途
- 权限声明越多，审核越敏感

模板建议：

- 只声明真正需要的能力
- 每个能力都要能说明业务理由

## 11. 微软商店上架注意事项

这一部分是模板项目最关键的交付经验。

### 11.1 商店上架不是“把 exe 传上去”

微软商店现代桌面应用提审核心是 `MSIX/APPX`，不是普通 exe 安装包。

因此后续项目要从一开始就按商店要求设计：

- 身份信息
- 图标资源
- 包结构
- 权限声明
- 多语言
- 截图与商店文案

### 11.2 Partner Center 的身份值必须回填到 Manifest

正式上架前，需要在 Partner Center 里获取正式信息，并回填到 `AppxManifest.xml.tpl`：

- 应用名称
- `Identity Name`
- `Publisher`

这一步不能凭感觉填写，必须严格一致。

### 11.3 本地证书仅用于侧载测试

本地开发时可以通过自签证书安装测试包，但它的用途仅限：

- 本机或测试机安装
- 提前验证安装行为

不要把本地测试签名当成正式上架配置。

### 11.4 图标资源不完整会直接出问题

商店和 `MSIX` 对资源完整性要求高。

常见问题：

- 缺少某个尺寸的图标
- 文件名不符合规则
- 图标边缘被裁切
- 高 DPI 显示模糊

模板已经通过脚本解决大部分问题，后续不要退回手工切图。

### 11.5 WebView2 运行时要提前验证

Tauri 应用依赖 WebView2 环境。

因此后续项目上线前要明确：

- 目标系统是否已自带 WebView2
- 你的分发方式是否需要额外声明依赖
- 在商店环境下是否需要补充对应配置

这个问题必须在正式发布前验证，不要留到审核阶段才发现。

### 11.6 数据写入位置必须符合 Windows 应用规范

尤其是在 `MSIX` 场景下，不要默认写入安装目录。

模板建议：

- 用户数据写应用数据目录
- 缓存写缓存目录
- 配置写用户侧可管理位置
- 不要依赖 exe 所在目录可写

### 11.7 权限声明越少越稳

原则很简单：

- 只申请必须权限
- 每个权限都能解释用途
- 能不用就不用

后续项目一旦涉及截图、系统级访问、文件系统扩展能力，更要提前确认商店是否允许、是否需要补充说明。

### 11.8 商店资料也属于发布工程的一部分

除了包本身，还要同步准备：

- 应用标题
- 副标题/简介
- 多语言商店描述
- 应用截图
- 图标
- 隐私政策链接
- 分类与关键词

建议把这些内容也纳入项目文档，而不是只放在某个人电脑里。

## 12. CI 与发布流程

当前仓库已在 `.github/workflows/release.yml` 中实现 `MSIX` 构建流程。

核心步骤是：

1. 拉代码
2. 安装 `pnpm`
3. 安装 Node
4. 安装 Rust
5. 安装依赖
6. `pnpm tauri build --no-bundle`
7. 执行 `stage.ps1`
8. 执行 `pack.ps1`
9. 上传 `.msix` 产物
10. tag 发布时挂到 GitHub Release

这说明模板项目已经具备：

- 自动构建
- 自动收集产物
- Release 附件输出

后续项目建议继续沿用：

- Windows runner
- 脚本化打包
- 固定产物目录
- Release artifact 输出

## 13. 文档与日志模板

你提到“其他项目一看这个文档，就知道架构，打包，log 等等事情”，这里建议把 `log` 明确拆成两类。

### 13.1 文档日志：开发决策与踩坑日志

当前仓库已经有：

- `docs/architecture.md`
- `docs/msix-packaging.md`
- `docs/dev-log/`

其中 `docs/dev-log/` 非常值得保留，因为它可以沉淀：

- 为什么选这个技术栈
- 为什么不用别的方案
- 某次打包遇到了什么坑
- 商店提交失败原因是什么
- 某项能力声明为什么必须保留

这类日志比临时聊天记录更适合模板继承。

建议后续项目保留固定文档结构：

- `architecture.md`
- `packaging.md`
- `release.md`
- `dev-log/`

### 13.2 应用日志：运行期日志

当前模板文档层面尚未专门定义运行期日志方案，但后续通用桌面项目建议尽快补上：

- 前端日志输出规范
- Rust 日志输出规范
- 日志文件存储目录
- 调试环境与生产环境日志级别

即使暂时没有实现专门日志模块，也建议至少在模板文档中预留这块。

建议的日志落地方式：

- 前端只记录界面行为、调用入口、异常摘要
- Rust 记录本地能力调用、文件路径、错误堆栈、性能关键步骤
- 日志按天或按大小滚动
- 日志目录放在用户应用数据目录下，例如 `AppData/Local/<YourApp>/logs/`
- 生产环境默认避免输出过多 debug 细节，防止日志膨胀
- 涉及隐私、文件内容、账号信息时要做脱敏

## 14. 新项目启动时的复用清单

后续如果要把当前仓库当模板起新项目，建议至少复用以下内容：

1. `src/` 与 `src-tauri/` 的双层结构
2. `tauri.conf.json` 的基本开发配置
3. `src-tauri/src/lib.rs` 的 command 注册方式
4. `scripts/msix/stage.ps1`
5. `scripts/msix/pack.ps1`
6. `scripts/msix/sign-dev.ps1`
7. `scripts/msix/generate-assets.ps1`
8. `src-tauri/msix/AppxManifest.xml.tpl`
9. `.github/workflows/release.yml`
10. `docs/architecture.md`、`docs/msix-packaging.md`、`docs/dev-log/`

## 15. 新项目初始化建议步骤

建议后续基于该模板创建新项目时，按下面步骤调整：

1. 替换应用名称、产品描述、包名
2. 修改 `tauri.conf.json` 中的 `productName`、`identifier`、版本信息
3. 修改 `AppxManifest.xml.tpl` 中的 Identity、Publisher、DisplayName、Description
4. 替换 `logo.png`
5. 执行 `pnpm icons:generate`
6. 校对 `src-tauri/msix/Assets/` 输出是否完整
7. 重新梳理 `src/views/`、`src/components/`、`src/services/`
8. 根据业务补充 Rust commands 与 services
9. 在测试机跑完整 `MSIX` 安装验证
10. 在正式上架前核对 Partner Center、图标、截图、多语言、隐私政策和权限声明

## 16. 结论

当前仓库最值得复用的，不是某个具体业务模块，而是整套 Windows 桌面工程模板能力：

- `Tauri 2 + Vue 3 + Rust` 的分层架构
- 清晰的目录组织方式
- 脚本化的 `MSIX` 打包链路
- 可复用的 `AppxManifest` 模板
- 可自动生成的 Logo/图标资源方案
- 可进入 CI 的发布流程
- 可沉淀的架构文档与开发日志体系

如果后续继续做 Windows 桌面程序，这一套完全可以作为标准模板使用。新项目只需要替换业务层和品牌资源，不需要再从零搭建桌面框架、MSIX 打包、图标生成和上架流程。
